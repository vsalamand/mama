
  <div class="d-flex align-items-center">
    <li class="h2 text-primary mr-1"><%= link_to category.name, category_path(category) %></li>

     <small class="badge badge-light border text-success"><%= category.get_store_section.name if category.get_store_section.present? %></small>
  </div>
