<%= simple_form_for [@recipe, @item] do |f| %>
  <%= f.input :recipe_ingredient, class: 'form-control' %>
  <%= f.collection_select :ingredient_id, Ingredient.all.order(:name), :id, :name, class: 'form-control' %>
  <%= f.submit "Valider", class: "form-control btn-death" %>
<% end %>
