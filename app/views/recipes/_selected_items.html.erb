<% recipe.sort_by_store_sections.each do |key, array| %>
  <% if array.any? %>
    <div id="<%= key.parameterize(separator: '') %>">
      <div id="<%= key.parameterize(separator: '') %>Header">
        <ul class="bg-dark border-0 list-group-item rounded-top d-flex align-items-center justify-content-between pl-1 pb-0 m-0" style="filter: brightness(1.40);">
          <div class="d-flex align-items-center">
            <p class="text-white m-0 p-1 font-weight-bold lead"><%= key %></p>
          </div>
        </ul>
      </div>
      <div class="recipeItem<%= recipe.id %>">
        <%  array.sort_by{|e| e.name.parameterize }.each do |item| %>
            <%= render 'items/selected_item', item: item %>
        <% end %>

      </div>
    </div>
  <% end %>
<% end %>
