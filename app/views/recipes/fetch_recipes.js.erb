if(document.getElementById('relatedRecipes')) {
  document.getElementById('relatedRecipes').innerHTML = ""
  document.getElementById('relatedRecipes').innerHTML = ("<%= j render'recipes/related_recipes', recipes: @recipes, list: @list, source_type: @source_type, source_id: @source_id %>");
} else {
  document.getElementById('recipeResults').innerHTML = "";
  document.getElementById('recipeResults').innerHTML = ("<%= j render'recipes/search', recipes: @recipes, list: @list, source_type: @source_type, source_id: @source_id %>");
}
