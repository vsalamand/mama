$('.modal').modal('hide');
$(document.body).removeClass('modal-open');
$('.modal-backdrop').remove();

const page = document.querySelector("#content-wrap");
page.innerHTML = ("<%= j render 'show' %>");

$("#flash").html('<%= j render "shared/flashes" %>');
