<div class="list-group">
  <% current_user.lists.each do |list| %>
    <%= link_to list.name.capitalize, add_to_list_recipe_path(@recipe, :list_id => list.id), class:"list-group-item list-group-item-action" %>
  <% end %>
  <%= link_to "Nouvelle liste", add_to_list_recipe_path(@recipe), class:"list-group-item list-group-item-action active" %>
</div>
