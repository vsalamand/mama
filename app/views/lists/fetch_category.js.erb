document.getElementById('contentShow').innerHTML = "";
document.getElementById('contentShow').innerHTML = ("<%= j render'categories/modal_show', category: @category, list: @list, recipes: @recipes %>");


var categoryName = "<%= @category.name %>"
var listId = "<%= @list.id %>"

fetchRecipes(listId, categoryName)

function fetchRecipes(listId, categoryName) {
  $.ajax({
    url: "/recipes/fetch_recipes",
    cache: false,
    dataType: 'script',
    data: {
        l: listId,
        q: categoryName,
        source: "category<%= @category.id %>"
        },
    success: function(){
    }
  });
}
