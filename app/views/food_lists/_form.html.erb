
  <%= simple_form_for @foodlist do |f| %>

    <%= f.input :name, input_html: { value: "Liste de courses (#{current_user.food_lists.where(food_list_type: "grocery_list").size + 1})" }, label: false, class: 'form-control'%>

    <%= f.submit "Continuer", class: "form-control btn-primary" %>
    <%= link_to 'Annuler', food_lists_path(@foodlists), class: "form-control btn-default" %>
  <% end %>
