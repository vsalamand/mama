<% if checklists.present? %>
  <% checklists.get_curated_lists.each do |checklist|  %>
      <%= link_to "#{checklist.name}", list_path(checklist, ref: (request.url + "?form"), l: list.id), class:"btn btn-sm btn-light border m-1" %>
  <% end %>
<% end %>
