
<div id="storeCartShow" data="<%= @store_cart.id %>">

  <div class="container d-flex justify-content-between mb-0 px-0" id="listStoreCart" data="<%= @list.id %>">
 <!--     <%= link_to 'Retour', store_carts_path(:list_id => @list.id), class:"btn close m-2" %>
     <%= link_to 'Quitter', root_path, class:"btn close m-2" %> -->
  </div>

   <div class="d-flex flex-column align-items-center justify-content-center p-1 mb-0">
     <%= link_to "#{@list.name}", list_path(@list), class:"btn btn-lg btn-primary border-0 mb-1", style:"white-space: normal;" %>

      <p class="m-0 lead text-center">Votre sélection <strong class="btn-outline-primary font-weight-bold pt-1"><%= @store_cart.store.name %></p>
   </div>


  <div class="container my-2">
    <div class="d-flex align-items-center justify-content-between p-1" id="storeCartType" data="list">
      <div>
      </div>
    </div>
  </div>

  <div id="storeCartContent">
  </div>

  <div style="padding: 50px;"></div>


  <div class="container fixed-bottom d-flex justify-content-between p-1">
     <%= link_to "Comparer le panier", store_carts_path(:list_id => @list.id), class:"btn btn-large btn-block btn-primary m-2 py-3", style:"font-size: 0.8rem; white-space: normal;" %>

     <%= link_to "Continuer", add_to_cart_store_cart_path( @store_cart), class:"btn btn-flat m-2 py-3 ", id:"loadCartBtn", style:"font-size: 0.8rem; white-space: normal;" %>
  </div>


</div>


  <div class="modal fade" id="loadCartModal" tabindex="-1" role="dialog" aria-labelledby="loadCartModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content bg-white">
        <div class="modal-header">
          <h5 class="modal-title text-dark" id="loadCartModalLabel">🛒 Panier</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body m-0 p-0" style="height: 50vh;overflow-y: auto;">

          <div class="d-flex flex-column align-items-center justify-content-center mt-5">
            <i id="modalSpinner" class="fas fa-circle-notch fa-spin text-primary mb-1"></i>
            <small id="modalSpinner" class="text-muted">Récupération des produits et des prix en cours...</small>
          </div>


        </div>
      </div>
    </div>
  </div>
