<div id="storeCartShow" data="<%= @store_cart.id %>">

  <div class="container d-flex justify-content-between mb-4 px-0" id="listStoreCart" data="<%= @list.id %>">
     <%= link_to 'Retour', store_carts_path(:list_id => @list.id), class:"btn close m-2" %>
     <%= link_to 'Fermer', root_path, class:"btn close m-2" %>
  </div>

   <div class="d-flex flex-column align-items-center justify-content-center p-1 mb-0">
      <p class="m-0 lead text-center">Votre sélection <strong class="btn-outline-primary font-weight-bold"><%= @store_cart.store.name %></strong> pour <strong class="btn-outline-primary font-weight-bold"><%= @list.name %></p>
   </div>


  <div class="container my-2">
    <div class="d-flex align-items-center justify-content-between p-1" id="storeCartType" data="list">
      <div>
      </div>
    </div>
  </div>

  <div class="container mb-3 px-0">
    <div class="shadow bg-white">
      <div class="list-group list-group-flush mb-2" id="storeCartItemsList">
        <% @store_cart_items.sort_by(&:item_id).each do |store_cart_item| %>
          <%= render 'store_cart_items/show', store_cart_item: store_cart_item, list: @list %>
        <% end %>
      </div>
    </div>
  </div>

<div style="padding: 50px;"></div>


<div class="container">
   <%= link_to "Continuer vos achats", add_to_cart_store_cart_path( @store_cart), class:"btn fixed-bottom btn-large btn-flat m-2 py-3 mx-5 font-weight-bold" %>
</div>


</div>
