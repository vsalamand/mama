
<%= form_tag(list_send_invite_path(@list), class: 'form-group mb-0') do |f| %>
  <div class="px-2 py-4 bg-white">
    <%= text_field_tag 'email', '', placeholder: "Email de la personne à inviter", type: "email", class:"form-control form-control-lg text-primary py-4 mb-3 rounded font-weight-bold" %>
    <div class="d-flex align-items-center justify-content-center">
      <%= submit_tag "submit",:value => "Envoyer un lien de partage", class:"btn btn-flat2 btn-lg font-weight-bold closeModal" %>
    </div>
  </div>
<% end %>

