<nav class="navbar sticky-top navbar-expand-md navbar-white bg-white d-flex shadow-sm mb-4 pt-0 pb-0">
  <a class="navbar-brand text-dark" href="/">😋 ClubMama</a>

  <% if params[:controller] == "food_lists" && params[:action] == "add" %>
    <%= link_to food_list_path(@foodlist), class: 'ml-auto p-2' do %>
      <button type="button" class="btn btn-sm btn-flat ml-auto p-2">
        Liste de courses <span class="badge badge-light grocerylist-count"> <%= @foodlist.foods.size %></span>
      </button>
    <% end  %>
  <% end %>

  <% if params[:controller] == "food_lists" && params[:action] == "show" %>
    <%= link_to get_cart_food_list_path(@foodlist), class: 'ml-auto p-2', target: :_blank do %>
      <button type="button" class="btn btn-sm btn-flat ml-auto p-2">
        Voir les prix <span class="badge badge-light grocerylist-count"> <%= @foodlist.foods.size %></span>
      </button>
    <% end  %>
  <% end %>

  <% if params[:controller] == "cart_items" && params[:action] == "show" %>
    <%= link_to cart_path(params[:cart_id]), class: 'ml-auto p-2' do %>
      <button type="button" class="btn btn-sm btn-flat ml-auto p-2">
        Retour au panier
      </button>
    <% end  %>
  <% end %>

</nav>
