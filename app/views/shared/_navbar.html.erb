<nav class="navbar sticky-top navbar-expand-lg navbar-white bg-white d-flex shadow-sm mb-4">
  <a class="navbar-brand text-dark" href="/">😋 FoodMama</a>

  <% if params[:controller] == "food_lists" && params[:action] == "add" %>
    <%= link_to food_list_path(@foodlist), class: 'ml-auto p-2' do %>
      <button type="button" class="btn btn-flat ml-auto p-2">
        📝 Envoyer la liste <span class="badge badge-light"> <%= @foodlist.foods.size %></span>
      </button>
    <% end  %>
  <% end %>

</nav>
