  <% user.carts.where.not(merchant: nil).each do |cart| %>

    <li class="pl-3 pr-3 list-group-item d-flex flex-column justify-content-start">
      <div class="d-flex align-items-center justify-content-between">
        <div>
          <h4> <%=  link_to cart.merchant.name.capitalize, cart_path(cart) %> </h4>

        </div>
        <div>
          <h4> <%= cart.get_total_price %>€</h4>
        </div>

      </div>
    </li>

  <% end %>
