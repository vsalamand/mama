<%= render 'shared/admin-navbar' %>

<div class="wrapper bg-white">
  <div class="container my-2 px-0">
    <%= render partial: 'users/show', collection: @users.sort.reverse, as: :user %>
  </div>
</div>
