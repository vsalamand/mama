<%= simple_form_for [@meta_recipe, @meta_recipe_item] do |f| %>
  <%= f.input :ingredient, class: 'form-control' %>
  <%= f.collection_select :food_id, Food.all.order(:name), :id, :name, class: 'form-control' %>
  <%= f.submit "Valider", class: "form-control btn-death" %>
<% end %>
