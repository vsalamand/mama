const item = document.querySelector("#store-cart-item-card-id<%= @store_cart_item.id %>");
item.innerHTML = "";
item.innerHTML = ("<%= j render 'store_cart_items/show', store_cart_item: @store_cart_item, store_item: @store_cart_item.store_item, list: @list %>");

// close modal is opened
$('.modal').modal('hide');
$(document.body).removeClass('modal-open');
$('.modal-backdrop').remove();
